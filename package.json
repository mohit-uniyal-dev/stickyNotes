{
  "name": "stickyNotes",
  "version": "1.1.0",
  "private": true,
  "description": "Dev tooling for the stickyNotes browser extension (not shipped in the extension package).",
  "scripts": {
    "check": "node tools/check.mjs",
    "zip": "node tools/zip.mjs"
  }
}
