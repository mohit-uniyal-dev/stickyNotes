# Normalize line endings: store as LF in git, check out LF everywhere.
* text=auto eol=lf

# Binary assets — never apply line-ending conversion.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
